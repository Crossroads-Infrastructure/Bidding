# Copy to .env.local and fill in once a Supabase project exists.
# Without these set, the app runs on a seeded in-memory store (see
# src/lib/repository/in-memory.ts) so it's usable without a live database.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
